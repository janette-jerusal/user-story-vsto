# User Story Similarity Add-In

Excel VSTO Add-in that compares user stories from two Excel files using cosine similarity.

## Setup
1. Install NuGet packages:
   ```powershell
   Install-Package EPPlus
